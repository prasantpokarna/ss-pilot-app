version: 0.2

phases:
  build:
    commands:
      - echo Entering Build Phase
      - docker --version
      - pwd
      - ls -a
      - docker build -t ss-pilot-app .

artifacts:
  files:
    - '**/*'
